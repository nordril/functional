using System;
using System.Collections.Generic;
using System.Text;

namespace Nordril.Functional.Algebra
{
    /*public struct TaggedMonoid<T, TOp> : ITaggedMonoid<T, TOp>
        where T : IMonoid<T>
    {
        public IMonoid<T> Value { get; }

        public TaggedMonoid(IMonoid<T> value)
        {
            Value = value;
            Value = value;
        }

        T IMonoid<T>.Neutral => Value.Neutral;

        public T Op(T that) => Value.Op(that);
    }*/
}
